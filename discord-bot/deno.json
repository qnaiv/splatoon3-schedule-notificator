{
  "tasks": {
    "dev": "deno run --allow-net --allow-env --unstable-cron --watch main.ts",
    "start": "deno run --allow-net --allow-env --unstable-cron main.ts"
  },
  "compilerOptions": {
    "allowJs": true,
    "lib": ["deno.window", "deno.unstable"],
    "strict": true
  }
}